# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Django migrations (optional, but usually included)
# Only ignore if you don't want to track migration history
# Or ignore if you generate migrations dynamically per environment
*/migrations/*.pyc
*/migrations/__pycache__/
*/migrations/*.py[cod]
# Don't ignore __init__.py files in migrations

# Environment / virtualenv
venv/
.env
.env.*

# IDEs / editors
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# OS files
# .DS_Store
# Thumbs.db

# Logs
*.log

# Database files (if using SQLite)
# *.sqlite3

# Static and media files (if you don't want to commit them)
# staticfiles/
# media/
